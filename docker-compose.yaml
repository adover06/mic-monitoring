services:
  server:
    container_name: mic-monitoring-server
    build: .
    restart: unless-stopped
    volumes:
      - ./data:/data
    networks:
      sce:
        aliases:
          - mic-monitoring-server.sce
          - mic-monitoring-server
networks:
  sce:
    external: true
    name: sce